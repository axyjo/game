<!DOCTYPE html>
<html>
<head>
  <title>Game</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="brand" href="#">
          GameName
        </a>
      </div>
      <% if current_user %>
        Welcome, <%= current_user.email %>! <a href="/logout">Log out</a>
      <% else %>
        <a href="/auth/google_oauth2">Login with Google</a>
      <% end %>
    </div>
  </div>
  <div id="container">
    <%= yield %>
  </div>
</body>
</html>
